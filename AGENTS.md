# Project Instructions

This project uses Python as its primary runtime.

Always use this interpreter for Python commands in this repository:

```text
C:\ProgramData\miniconda3\envs\qgis\python.exe
```

When running Python scripts, tests, package commands, or one-off checks, prefer the explicit interpreter path above instead of `python`, `py`, or another Conda environment.

